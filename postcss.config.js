/** @type {import('tailwindcss').Config} */
module.exports = {
    darkMode: 'media', // o 'class' se vuoi un interruttore manuale
    theme: {
        extend: {},
    },
    plugins: {
        '@tailwindcss/postcss': {},
    },
};